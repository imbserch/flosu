// An AudioTrack is just a string (path to the audio file), because each track is loaded once
typedef AudioTrack = String;

// A Sample is just a string (path to the audio file), because each sample is loaded once
typedef Sample = String;
