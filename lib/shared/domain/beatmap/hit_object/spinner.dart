part of 'hit_object.dart';

/// A spinnable object that the player must spin.
/// It's a special object that doesn't have a combo number, and it's always centered at the playfield.
class Spinner extends HitObjectWithEndTime {
  Spinner() : super();
}
